<svg fill="none" viewBox="0 0 1000 1000" width="100%" height="100%" xmlns="http://www.w3.org/2000/svg">
  <foreignObject width="100%" height="100%">
    <div xmlns="http://www.w3.org/1999/xhtml">
      <style>
        * { box-sizing: border-box; margin: 0; padding: 0; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; }
        body, html { width: 100vw; height: 100vh; overflow: hidden; background: #0f141c; color: #fff; }

        /* Top Bar */
        .top-bar {
          display: flex;
          justify-content: space-between;
          align-items: center;
          padding: 14px 24px;
          background: #182232;
          border-bottom: 1px solid #28374d;
        }
        .logo { font-size: 1.2rem; font-weight: bold; color: #4fa3ff; }
        .search-box {
          width: 350px;
          padding: 8px 14px;
          background: #0f141c;
          border: 1px solid #28374d;
          border-radius: 6px;
          color: #fff;
          outline: none;
        }

        /* Game Grid */
        .grid-container {
          padding: 24px;
          height: calc(100vh - 65px);
          overflow-y: auto;
          display: grid;
          grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
          gap: 16px;
        }
        .game-card {
          background: #182232;
          border: 1px solid #28374d;
          border-radius: 10px;
          overflow: hidden;
          cursor: pointer;
          transition: transform 0.15s ease, border-color 0.15s ease;
        }
        .game-card:hover {
          transform: translateY(-4px);
          border-color: #4fa3ff;
        }
        .game-thumb {
          width: 100%;
          height: 140px;
          object-fit: cover;
          display: block;
          background: #000;
        }
        .game-title {
          padding: 10px;
          font-size: 0.85rem;
          font-weight: 600;
          text-align: center;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }

        /* Player View */
        #game-viewer {
          display: none;
          position: fixed;
          top: 0; left: 0;
          width: 100vw; height: 100vh;
          background: #000;
          z-index: 999;
          flex-direction: column;
        }
        .viewer-nav {
          display: flex;
          justify-content: space-between;
          padding: 8px 16px;
          background: #111;
          border-bottom: 1px solid #222;
        }
        .viewer-nav button {
          background: #222;
          color: #fff;
          border: 1px solid #444;
          padding: 6px 12px;
          border-radius: 4px;
          cursor: pointer;
        }
        iframe { flex: 1; border: none; width: 100%; height: 100%; }
      </style>

      <div class="top-bar">
        <div class="logo">Vault</div>
        <input type="text" id="search" class="search-box" placeholder="Search games..." />
      </div>

      <div class="grid-container" id="grid"></div>

      <div id="game-viewer">
        <div class="viewer-nav">
          <button onclick="closeGame()">← Back</button>
          <span id="game-label" style="line-height: 28px; font-weight: bold;"></span>
          <button onclick="toggleFS()">Fullscreen</button>
        </div>
        <iframe id="frame" src=""></iframe>
      </div>

      <script>
        // Game Catalog Array
        const library = [
          {
            name: "Metroid Fusion",
            thumb: "https://images.igdb.com/igdb/image/upload/t_cover_big/co1xau.png",
            url: "https://emulatorjs.org/demo/?core=gba"
          },
          {
            name: "Wario Land 4",
            thumb: "https://images.igdb.com/igdb/image/upload/t_cover_big/co1xaz.png",
            url: "https://emulatorjs.org/demo/?core=gba"
          },
          {
            name: "Spacebar Clicker",
            thumb: "https://via.placeholder.com/200/182232/4fa3ff?text=Spacebar",
            url: "https://spacebar-clicker.com"
          }
        ];

        const grid = document.getElementById('grid');
        const search = document.getElementById('search');
        const viewer = document.getElementById('game-viewer');
        const frame = document.getElementById('frame');
        const label = document.getElementById('game-label');

        function render(items) {
          grid.innerHTML = '';
          items.forEach(item => {
            const el = document.createElement('div');
            el.className = 'game-card';
            el.innerHTML = `
              <img class="game-thumb" src="${item.thumb}" loading="lazy" />
              <div class="game-title">${item.name}</div>
            `;
            el.onclick = () => openGame(item);
            grid.appendChild(el);
          });
        }

        function openGame(item) {
          label.innerText = item.name;
          frame.src = item.url;
          viewer.style.display = 'flex';
        }

        function closeGame() {
          viewer.style.display = 'none';
          frame.src = '';
        }

        function toggleFS() {
          if (!document.fullscreenElement) {
            frame.requestFullscreen();
          } else {
            document.exitFullscreen();
          }
        }

        search.addEventListener('input', (e) => {
          const val = e.target.value.toLowerCase();
          render(library.filter(i => i.name.toLowerCase().includes(val)));
        });

        render(library);
      </script>
    </div>
  </foreignObject>
</svg>
