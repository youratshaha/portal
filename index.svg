<svg xmlns="http://www.w3.org/2000/svg" width="100vw" height="100vh" style="width: 100vw; height: 100vh; background: #0f141c; position: fixed; top: 0; left: 0; margin: 0; padding: 0;">
  <foreignObject width="100%" height="100%">
    <div xmlns="http://www.w3.org/1999/xhtml" style="width: 100vw; height: 100vh; overflow-x: hidden; overflow-y: auto; background: #0f141c; color: #fff; margin: 0; padding: 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;">
      <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body { background: #0f141c; color: #fff; }

        /* Top Bar */
        .top-bar {
          display: flex;
          justify-content: space-between;
          align-items: center;
          padding: 14px 28px;
          background: #182232;
          border-bottom: 1px solid #28374d;
          position: sticky;
          top: 0;
          z-index: 10;
        }
        .logo { font-size: 1.3rem; font-weight: 800; color: #4fa3ff; letter-spacing: 0.5px; }
        .search-box {
          width: 400px;
          max-width: 50%;
          padding: 10px 16px;
          background: #0f141c;
          border: 1px solid #28374d;
          border-radius: 8px;
          color: #fff;
          outline: none;
          font-size: 0.9rem;
        }
        .search-box:focus { border-color: #4fa3ff; }

        /* Game Grid */
        .grid-container {
          padding: 28px;
          display: grid;
          grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
          gap: 20px;
          max-width: 1400px;
          margin: 0 auto;
        }
        .game-card {
          background: #182232;
          border: 1px solid #28374d;
          border-radius: 12px;
          overflow: hidden;
          cursor: pointer;
          transition: transform 0.2s ease, border-color 0.2s ease;
          display: flex;
          flex-direction: column;
        }
        .game-card:hover {
          transform: translateY(-4px);
          border-color: #4fa3ff;
        }
        .game-thumb {
          width: 100%;
          height: 150px;
          object-fit: cover;
          display: block;
          background: #000;
        }
        .game-info {
          padding: 12px;
          text-align: center;
        }
        .game-title {
          font-size: 0.9rem;
          font-weight: 600;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }

        /* Player View */
        #game-viewer {
          display: none;
          position: fixed;
          top: 0; left: 0;
          width: 100vw; height: 100vh;
          background: #000;
          z-index: 999;
          flex-direction: column;
        }
        .viewer-nav {
          display: flex;
          justify-content: space-between;
          align-items: center;
          padding: 10px 20px;
          background: #111;
          border-bottom: 1px solid #222;
        }
        .viewer-nav button {
          background: #222;
          color: #fff;
          border: 1px solid #444;
          padding: 6px 14px;
          border-radius: 6px;
          cursor: pointer;
          font-weight: 600;
        }
        .viewer-nav button:hover { background: #333; }
        iframe { flex: 1; border: none; width: 100%; height: 100%; }
      </style>

      <!-- Main Hub -->
      <div class="top-bar">
        <div class="logo">Vault</div>
        <input type="text" id="search" class="search-box" placeholder="Search library..." />
      </div>

      <div class="grid-container" id="grid"></div>

      <!-- In-Page Player -->
      <div id="game-viewer">
        <div class="viewer-nav">
          <button onclick="closeGame()">← Back</button>
          <span id="game-label" style="font-weight: bold; color: #fff;"></span>
          <button onclick="toggleFS()">Fullscreen</button>
        </div>
        <iframe id="frame" src=""></iframe>
      </div>

      <script type="text/javascript">
      // <![CDATA[
        const library = [
          {
            name: "Metroid Fusion",
            thumb: "https://images.igdb.com/igdb/image/upload/t_cover_big/co1xau.png",
            url: "https://emulatorjs.org/demo/?core=gba"
          },
          {
            name: "Wario Land 4",
            thumb: "https://images.igdb.com/igdb/image/upload/t_cover_big/co1xaz.png",
            url: "https://emulatorjs.org/demo/?core=gba"
          },
          {
            name: "Diablo",
            thumb: "https://images.igdb.com/igdb/image/upload/t_cover_big/co20u5.png",
            url: "https://d07riv.github.io/diabloweb/"
          },
          {
            name: "Spacebar Clicker",
            thumb: "https://images.igdb.com/igdb/image/upload/t_cover_big/co1r7h.png",
            url: "https://spacebar-clicker.com"
          }
        ];

        const grid = document.getElementById('grid');
        const search = document.getElementById('search');
        const viewer = document.getElementById('game-viewer');
        const frame = document.getElementById('frame');
        const label = document.getElementById('game-label');

        function render(items) {
          grid.innerHTML = '';
          items.forEach(function(item) {
            const el = document.createElement('div');
            el.className = 'game-card';
            el.innerHTML = '<img class="game-thumb" src="' + item.thumb + '" loading="lazy" />' +
                           '<div class="game-info"><div class="game-title">' + item.name + '</div></div>';
            el.onclick = function() { openGame(item); };
            grid.appendChild(el);
          });
        }

        function openGame(item) {
          label.innerText = item.name;
          frame.src = item.url;
          viewer.style.display = 'flex';
        }

        function closeGame() {
          viewer.style.display = 'none';
          frame.src = '';
        }

        function toggleFS() {
          if (!document.fullscreenElement) {
            frame.requestFullscreen();
          } else {
            document.exitFullscreen();
          }
        }

        search.addEventListener('input', function(e) {
          const val = e.target.value.toLowerCase();
          render(library.filter(function(i) {
            return i.name.toLowerCase().indexOf(val) !== -1;
          }));
        });

        render(library);
      // ]]>
      </script>
    </div>
  </foreignObject>
</svg>
